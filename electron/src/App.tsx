import React from "react";
import "./App.scss";

const App: React.FC = () => {
  return (
    <div className="app">
      <header className="app-header">
        <h1>Electron + React!</h1>
        <p>🎉 Your Electron app with React, TypeScript, and SCSS is working!</p>
      </header>
    </div>
  );
};

export default App;
